spring:
  application:
    name: event-stream-dashboard

  datasource:
    url: jdbc:h2:mem:eventsdb
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  h2:
    console:
      enabled: true
      path: /h2-console